# 🚀 Автоматическое обновление Git репозитория

## 📦 Что в архиве:

- **git_update.py** - Python скрипт для автоматического обновления
- **oauth_server.py** - Обновленный OAuth сервер с VK
- **handlers/** - Обновленные handlers с VK Integration

---

## 📝 Инструкция:

### Шаг 1: Распакуй архив

Распакуй `GIT_AUTO_UPDATE.zip` в папку твоего проекта на компьютере.

**Структура должна быть:**
```
твой-проект/
├── git_update.py          ← Новый скрипт
├── oauth_server.py        ← Заменит старый
├── handlers/              ← Заменит старые handlers
│   ├── vk_integration/    ← Новая папка
│   └── ...
├── main.py                ← Твои существующие файлы
├── requirements.txt
└── .git/                  ← Git репозиторий
```

---

### Шаг 2: Запусти скрипт

**Windows:**
```cmd
python git_update.py
```

**Mac/Linux:**
```bash
python3 git_update.py
```

---

### Шаг 3: Следуй инструкциям скрипта

Скрипт спросит:

**1. Путь к проекту:**
```
Введите путь к проекту (или нажмите Enter для текущей директории):
```
- Нажми **Enter** если запустил в папке проекта
- Или укажи полный путь: `C:\Users\User\project`

**2. Сообщение коммита:**
```
Введите сообщение коммита (Enter = 'Add VK OAuth support'):
```
- Нажми **Enter** для стандартного сообщения
- Или введи свое: `Добавлен VK OAuth`

**3. Подтверждение push:**
```
Отправить изменения в origin/main? (y/n):
```
- Введи **y** для отправки
- Введи **n** чтобы остановиться (файлы будут закоммичены локально)

---

### Шаг 4: Дождись завершения

Скрипт выполнит:
```
✅ git add oauth_server.py handlers/
✅ git commit -m "Add VK OAuth support"
✅ git push origin main
```

---

### Шаг 5: Проверь на Render.com

1. Зайди на Render.com Dashboard
2. Проект должен автоматически начать деплой
3. Дождись завершения (~2-5 минут)
4. Проверь логи на наличие ошибок

---

### Шаг 6: Проверь работу

Открой в браузере:
```
https://alexshylman-sudo-project.onrender.com/health
```

**Должно вернуть:**
```json
{
  "status": "ok",
  "service": "pinterest-vk-oauth-server",
  "pinterest_configured": true,
  "vk_configured": true,
  "database": "connected"
}
```

**Если видишь `vk_configured: true` - всё работает!** 🎉

---

## 🐛 Возможные проблемы:

### "git: command not found"

**Причина:** Git не установлен

**Решение:** 
- Windows: Скачай Git с https://git-scm.com/
- Mac: `brew install git`
- Linux: `sudo apt install git`

---

### "Permission denied (publickey)"

**Причина:** Не настроена авторизация в GitHub

**Решение:**
1. Настрой SSH ключ: https://docs.github.com/en/authentication
2. Или используй HTTPS и введи логин/токен при push

---

### "nothing to commit, working tree clean"

**Причина:** Файлы не изменились или уже закоммичены

**Решение:** Проверь что файлы из архива отличаются от существующих

---

### Скрипт не находит файлы

**Причина:** Запущен не в папке проекта

**Решение:** 
1. Открой командную строку/терминал
2. Перейди в папку проекта: `cd /path/to/project`
3. Запусти скрипт: `python git_update.py`

---

## ✅ Checklist после обновления:

- [ ] Скрипт завершился без ошибок
- [ ] На GitHub появился новый коммит "Add VK OAuth support"
- [ ] Render.com начал деплой
- [ ] `/health` показывает `vk_configured: true`
- [ ] В логах есть "✅ VK Integration загружен"
- [ ] В боте появилась кнопка "Подключить VK"

---

## 🆘 Нужна помощь?

Если что-то не работает - проверь логи:

**На компьютере:**
Скрипт покажет вывод Git команд

**На Render.com:**
Dashboard → Logs → ищи ошибки

**В боте:**
Попробуй подключить VK и посмотри что происходит

---

## 🎉 Готово!

После успешного обновления VK OAuth будет работать!

**Удачи!** 🚀
