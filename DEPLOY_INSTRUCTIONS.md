# 🚀 Инструкция по развертыванию VK OAuth

## ✅ Что в этом архиве:

### Обновленные файлы:
- **oauth_server.py** - OAuth сервер с поддержкой Pinterest И VK
- **handlers/__init__.py** - импорт VK Integration
- **handlers/vk_integration/** - модуль VK OAuth
- **handlers/auto_publish_scheduler.py** - исправлен автопостинг
- **handlers/platform_connections/vk.py** - поддержка групп и личных страниц

---

## 📝 Шаги для развертывания:

### Шаг 1: Загрузи файлы на сервер

Замени эти файлы на сервере (Render.com):

```
oauth_server.py                           ← ГЛАВНЫЙ ФАЙЛ
handlers/__init__.py
handlers/vk_integration/                  ← Вся папка
handlers/auto_publish_scheduler.py
handlers/platform_connections/vk.py
handlers/platform_settings/               ← Вся папка
handlers/platform_category/images_menu.py
handlers/telegram_images_settings.py
handlers/pinterest_images_settings.py
```

---

### Шаг 2: Проверь переменные окружения

На Render.com в разделе **Environment** должны быть:

```env
VK_APP_ID=54433963
VK_APP_SECRET=твой_секретный_ключ
PORT=10000
```

---

### Шаг 3: Перезапусти сервис

На Render.com нажми:
**"Manual Deploy"** → **"Clear build cache & deploy"**

---

### Шаг 4: Проверь работу

#### 1. Проверь health endpoint:
```
https://alexshylman-sudo-project.onrender.com/health
```

Должно вернуть:
```json
{
  "status": "ok",
  "service": "pinterest-vk-oauth-server",
  "pinterest_configured": true,
  "vk_configured": true,
  "database": "connected"
}
```

#### 2. Проверь логи:
В логах Render.com должны быть:
```
✅ VK Integration Package загружен
✅ Database connected
🚀 Starting OAuth Server on port 10000
```

#### 3. Проверь в боте:
Зайди в Telegram бот и найди кнопку:
```
🔵 Подключить VK
```

---

## 🎯 Как работает VK OAuth:

### Шаг 1: Пользователь нажимает "Подключить VK"

Бот показывает меню выбора типа:
```
💬 ПОДКЛЮЧЕНИЕ ВКОНТАКТЕ

Шаг 1 из 3: Выберите тип

Куда вы хотите публиковать посты?

[👥 Группа/Сообщество]
[👤 Личная страница]
[❌ Отмена]
```

### Шаг 2: Выбирает тип и вводит ID

Например:
```
https://vk.com/xoverx
```

### Шаг 3: Получает токен

Бот дает ссылку для получения токена (через старое VK API):
```
[🔑 Получить токен]
```

Пользователь:
1. Нажимает кнопку
2. Авторизуется в VK
3. Копирует токен из URL
4. Отправляет боту

### Шаг 4: Готово!

```
✅ Личная страница VK подключена!
🆔 ID: xoverx
```

---

## 🔧 Два способа подключения VK:

### Способ 1: Упрощенный (через токен) ✅
**Файл:** `handlers/platform_connections/vk.py`

**Как работает:**
- Пользователь получает токен по ссылке
- Копирует и вставляет в бота
- Просто и надежно

**Используется сейчас!**

### Способ 2: OAuth через webhook 🔧
**Файл:** `oauth_server.py` endpoint `/vk_callback`

**Как работает:**
- Пользователь нажимает кнопку
- VK перенаправляет на webhook
- Токен сохраняется автоматически

**Готов, но требует настройки VK ID приложения**

---

## 🐛 Возможные проблемы:

### "VK Integration не загружен"

**Причина:** Не добавлен импорт в `handlers/__init__.py`

**Решение:** Открой `handlers/__init__.py` и добавь в конец:
```python
from handlers.vk_integration import *
```

---

### "No module named 'ai.claude_client'"

**Причина:** Старый импорт в автопостинге

**Решение:** Уже исправлено в `handlers/auto_publish_scheduler.py` из этого архива

---

### VK OAuth не работает

**Причина:** Не настроено VK ID приложение

**Решение:** 
1. Используй упрощенный способ (через токен) - он работает!
2. Или настрой VK ID согласно `VK_OAUTH_DEPLOY.md`

---

## ✅ Checklist:

- [ ] Файлы загружены на сервер
- [ ] `VK_APP_SECRET` добавлен в Environment
- [ ] `handlers/__init__.py` обновлен (добавлен импорт VK)
- [ ] Сервис перезапущен
- [ ] `/health` показывает `vk_configured: true`
- [ ] В боте есть кнопка "Подключить VK"

---

## 🎉 Готово!

После выполнения всех шагов VK OAuth будет работать!

Если возникнут проблемы - проверь логи на Render.com.

**Удачи!** 🚀
